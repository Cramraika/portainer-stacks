# uptime-kuma — no compose-level env vars (compose carries no ${VAR} interpolation).
# File present to satisfy the per-stack contract (CLAUDE.md §2.1: compose + README + .env.example).
# If env vars are introduced, name them UPTIME_KUMA_<NOUN> per §2.2; non-secret defaults only —
# real secrets render via Infisical at deploy time (never in this repo, never pasted into the Portainer UI).
